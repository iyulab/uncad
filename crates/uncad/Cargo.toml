[package]
name = "uncad"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme.workspace = true
description = "CAD file (DWG/DXF) parsing, SVG rendering, and DWG/DXF writing, built on libredwg-sys."
keywords = ["cad", "dwg", "dxf", "svg", "autocad"]
categories = ["graphics", "parser-implementations"]

[lints]
workspace = true

[dependencies]
libredwg-sys = { path = "../libredwg-sys" }
libc = "0.2"
regex = "1"
resvg = "0.48"
