[package]
name = "libredwg-sys"
version = "0.2.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme.workspace = true
description = "Raw FFI bindings to the LibreDWG C sources (a vendored subset of upstream carrying five local patches, see NOTICE.md), built natively via cc + bindgen (no Emscripten/WASM)."
keywords = ["dwg", "dxf", "ffi", "libredwg", "cad"]
categories = ["external-ffi-bindings"]
links = "libredwg"

[lints]
workspace = true

[build-dependencies]
cc = "1"
bindgen = "0.73"
